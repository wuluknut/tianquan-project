spring:
  docker:
    compose:
      enabled: true
      file: @basedir@/compose.yaml
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_DATABASE:tianquan}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
